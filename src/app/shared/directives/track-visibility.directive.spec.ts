import { TrackVisibilityDirective } from './track-visibility.directive';

describe('TrackVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
